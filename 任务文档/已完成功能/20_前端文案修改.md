# 前端文案修改 (Frontend Text Update)

## 需求说明
将聊天界面中AI正在生成的提示文案从"AI 正在思考..."/"正在思考..."修改为"AI 正在运行中..."/"正在运行中..."。

## 实现细节
1. 修改 `src/app/chat/[id]/chat-page-client.tsx` 里的 `getStatusText` 方法，把对应文案替换为相应的运行中提示。
2. 修改 `src/components/chat/chat-area.tsx` 里的 `getStatusText` 方法，替换思考中文案。

## 验证
已通过部署脚本将其更新到远程并完成构建。
